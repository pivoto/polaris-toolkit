#parse("/vm/include.vm")
package ${pkgPrefix}${env.controllerPackage};

import ${pkgPrefix}${env.entityPackage|"entity"}.${table.javaClassName};
import ${pkgPrefix}${env.servicePackage|"service"}.${table.javaClassName}Service;

import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.Description;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.GetMapping;

import java.util.*;

/**
 * $!{table.name}
 * $!{table.comment}
 * @author $!{env.author}
 */
@Slf4j
@Controller
@RequestMapping(value = "$!{env.controllerPackage.concat("/")}${table.xmlName}")
public class ${table.javaClassName}Controller{

	@Autowired
	private ${table.javaClassName}Service ${table.javaVariableName}Service;

}

